<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    package="com.akexorcist.example.feature_blogger"
    split="feature_blogger">

    <uses-permission android:name="android.permission.INTERNET" />

    <dist:module
        dist:onDemand="true"
        dist:title="@string/title_feature_blogger">
        <dist:fusing dist:include="true" />
    </dist:module>

    <application>
        <activity
            android:name=".ui.blogger.BloggerActivity"
            android:label="@string/profile_on_blogger" />
    </application>

</manifest>

